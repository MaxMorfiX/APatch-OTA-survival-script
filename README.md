# APatch-OTA-survival-addon.d-script
APatch OTA Survival for A-only devices This addon.d script ensures APatch survives LineageOS OTA updates on A-only devices (tested on Mi 9 Lite / pyxis). It extracts the root superkey from the current kernel and re-patches the new boot image after the update. 
